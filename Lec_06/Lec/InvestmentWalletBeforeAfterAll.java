public class InvestmentWalletBeforeAfterAllTest {
    @BeforeAll 
    static void setUpTestCase() {
        // called only once before all the method tests are executed, it is used for expensive operations
    }

    @AfterAll 
    static void tearDownTestCase() {
        // called only after all the methods tests are executed, 
    }
}